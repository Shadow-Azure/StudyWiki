{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "All windows: core defaults plus folder picking. File access goes through the two custom commands and the asset protocol.",
  "windows": ["main", "win-*"],
  "permissions": ["core:default", "dialog:default"]
}
