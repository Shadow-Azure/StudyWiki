{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: core defaults plus folder picking. File access goes through the two custom commands and the asset protocol.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
