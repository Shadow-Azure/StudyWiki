# Tauri 命令目录

[English](commands.en.md) | 中文

> 类型：参考 | 层级：API 目录。生成区由 `pnpm gen:commands` 从 `src-tauri/src/lib.rs` 生成，区块内禁手改；新鲜度由 doc-sync 档校验。

<!-- BEGIN GENERATED commands-catalog (scripts/gen-commands-catalog.mjs) — do not edit between markers -->
```rust
// Generated by scripts/gen-commands-catalog.mjs from src-tauri/src/lib.rs — do not edit.
// Registered: list_library, read_text_file

/// Lists every openable entry (markdown note or playable video) directly
/// inside the opened library root, sorted case-insensitively by file name.
/// Files whose extension is neither are skipped silently. Errors carry the
/// OS failure verbatim (root unopenable, directory entry unreadable).
#[tauri::command]
fn list_library(root: String) -> Result<Vec<LibraryEntry>, String>;

/// Reads a whole file as a UTF-8 string — the markdown viewer's data source.
/// Errors carry the OS failure verbatim.
#[tauri::command]
fn read_text_file(path: String) -> Result<String, String>;

```
<!-- END GENERATED commands-catalog -->

前端经 `@tauri-apps/api` 的 `invoke` 调用；参数与返回形状以签名为契约。命令面变化（新增/改签名/改注释）后跑 `pnpm gen:commands` 同步本页两侧，再配对重录。
