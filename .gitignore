# Node / frontend
node_modules/
# fixtures 里的 node_modules 是门禁自测的固定样本目录，不是真实安装目录（精确豁免，勿放宽）
!scripts/__fixtures__/**/node_modules/
dist/

# Rust / Tauri
src-tauri/target/
src-tauri/gen/

# Editors & OS
.DS_Store
*.pem

# Local env — the shipped client must not vary by environment
.env
.env.*

# 插件脚手架生成物（作者工作区，不进仓库）
plugins-dev/

# SDD per-plan scratch (ledger, briefs, review packages)
.superpowers/
